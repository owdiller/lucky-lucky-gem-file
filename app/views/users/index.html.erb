<h1>Ideas</h1>

<p><a href="/new_user">Add a New Idea</a></p>

<table>
  <thead>
    <tr>
   
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
          <td><a href="/users/<%= user.id %>">Show</a>
        </td>
      <td><a href="/users/<%= user.id %>/edit">Edit</a></td>
      <td><a href="/users/<%= user.id %>/destroy">Destroy</a></td>
    </tr>
  <% end %>
  </tbody>
</table>

